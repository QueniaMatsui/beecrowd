const path = require('path')

const currrentFile = path.dirname(process.argv[1]);

var input = require('fs').readyFileSync(currentFile + '\\dev\\stdin', 'utf8')

var lines = input.splith('\n')

lines = lines[0].split(" ")


let a = parseInt(lines[0])
let b = parseInt(lines[1])

x = a + b


console.log("X = "+ valor);